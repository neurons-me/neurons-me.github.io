import ace, { IAceEditorProps } from "react-ace/lib/ace";
import diff, { IDiffEditorProps, IDiffEditorState } from "react-ace/lib/diff";
import split, { ISplitEditorProps } from "react-ace/lib/split";
import { IAceOptions, IAnnotation, ICommand, ICommandBindKey, ICommandManager, IEditorProps, IMarker } from "react-ace/lib/types";
export { split, diff, IAceOptions, IAceEditorProps, IAnnotation, ICommand, ICommandBindKey, ICommandManager, IDiffEditorProps, IDiffEditorState, IEditorProps, IMarker, ISplitEditorProps };
export default ace;
